Nguyên tắc Inject (Dặn Claude Code):Main Camera (ID 0) dùng key gốc (không có suffix hoặc suffix _key).Tele Crop (Zoom 2x từ Main) dùng suffix _key_2. Bắt buộc: Toàn bộ thông số Patcher của Main phải được clone y hệt sang _key_2 để tránh gãy thuật toán khi zoom qua mốc 2x.Ultrawide (ID 3) dùng suffix _key_3.Handoff Spec: LMC 8.8 XML Patcher Overhaul cho Mi 10S1. Xử lý Main Camera & Tele Crop (S5KHMX 108MP)Mục tiêu: Trị bệnh ám Cyan vùng shadow, cứu chi tiết vùng đen (crushed blacks), giữ hạt Luma (grain), chặn thuật toán nội suy làm gai viền ảnh.Khối UI (Block)XML Key Target (Regex/Pattern)Giá trị cần Inject (Value)Bản chất Kỹ thuật (Rationale)Sabre / Spatial RGBlib_spatial_a_key (hoặc key quản lý Fix Noise Sabre)0.125Hardcode noise floor cho Sabre. Chặn thuật toán đoán sai nhiễu của Google trên cảm biến 108MP gây bệt chi tiết.Chroma Denoise (All merge)lib_chromadl... (Low)lib_chromadm... (Med)1.2 đến 1.3 (Tăng 20-30% so với def)Giết triệt để dải nhiễu đốm cyan/magenta ở vùng tối tần số thấp do S5KHMX sinh ra.Luma Denoise (Merge 1 sabre)lib_lumadlsabre...lib_lumadmsabre...0.85 (Giảm 15% so với def)Giữ lại hạt nhiễu (grain) đen trắng tự nhiên để che lấp khiếm khuyết màu sắc, tạo texture bề mặt cho ảnh.Color (Shadow Saturation)lib_colsatparam_shadow_ldr_key0.8Hút cạn bão hòa màu thừa (ám cyan) ở những vùng tiệm cận dải Black Level 64.Sharpness (Polysharp/RAISR)Lệnh quản lý Polysharp Radius và RAISRSet Polysharp Radius về mức nhỏ nhất. Disable RAISR.Chống viền halo (ringing artifacts). Để Sabre và Hexagon DSP tự lo việc tái tạo chi tiết.Gamma / Tone CurvesKey quản lý Parametric Curve -> Shadows (Lower Half)Tăng lên +3 hoặc +4 (Tương đương kích hoạt mode manual và gán hệ số)Nhấc nhẹ dải Shadow lên để bộc lộ chi tiết vùng tối (vân gỗ, vải đen) bị nén bởi Black Level 64.2. Xử lý Ultrawide Camera (OV13B10 13MP)Mục tiêu: Cắt đứt sự phụ thuộc sai lệch vào hằng số của Main Camera, tối ưu khử nhiễu nặng cho cảm biến nhỏ.Khối UI (Block)XML Key Target (Regex/Pattern)Giá trị cần Inject (Value)Bản chất Kỹ thuật (Rationale)White Balance Constantscw_rg_key_3cw_bg_key_3gr_key_3bg_key_3h_bg_key_3DELETE / NULL(Xóa hẳn các key này khỏi slot 3)S5KHMX và OV13B10 khác nhau hoàn toàn về quang học. Xóa hằng số fix cứng này để AWB profile 59 của UW tự chạy logic tính toán màu của nó, chống ám tím/lục.Luma Denoise (Merge 1 sabre)lib_lumadlsabre_..._key_3lib_lumadmsabre_..._key_31.5 đến 1.8 (Tăng mạnh)Cảm biến 13MP rất bé, tín hiệu nhiễu sáng cực cao. Phải cà Luma mạnh tay hơn Main Camera.Chroma Denoise (All merge)lib_chromadl..._key_3lib_chromadm..._key_31.5 (Tăng mạnh)Khống chế rác màu bệt ở vùng góc mép ảnh của ống kính Ultrawide.Lights and shadowsKey quản lý Highlight Fix / Shadow LDR cho Slot 3Tăng Shadow LDR nhẹDải nhạy sáng (Dynamic Range) của OV13B10 rất kém. Cần bù đắp bằng cách kích nhẹ thuật toán HDR vùng shadow.